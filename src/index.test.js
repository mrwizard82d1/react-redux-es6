/**
 * Created by larryjones on 4/21/17.
 */

// Canary test for `index.js (for now)

import expect from "expect";

describe('index.js', () => {
  it('should pass this canary test', () => {
    expect(true).toBe(false);
  });
});
